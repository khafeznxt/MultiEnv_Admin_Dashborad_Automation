
name: Run Selenium Test

on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on:
    steps:
       - uses: actions/checkout@v2
       
         with:
           java-version: "11"
           cache: maven
       - uses: browser-actions/setup-chrome@latest
       - run: mvn clean test